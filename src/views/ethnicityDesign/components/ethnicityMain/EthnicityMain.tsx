import { defineComponent } from "vue";

export default defineComponent({
  name: "EthnicityMain",
  setup: (props) => {
    return () => {
      return (
        <>
          <p>123</p>
        </>
      );
    };
  },
});
