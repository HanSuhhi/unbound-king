import { defineComponent } from "vue";

export default defineComponent({
  name: "SkillDesign",
  setup: (props) => {
    return () => {
      return (
        <p>
          <p>hello world</p>
        </p>
      );
    };
  },
});
