import { defineComponent } from "vue";

export default defineComponent({
  name: "CharacterDesign",
  setup: (props) => {
    return () => {
      return (
        <div>
          <span>1231231</span>
        </div>
      );
    };
  },
});
